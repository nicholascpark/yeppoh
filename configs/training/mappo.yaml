# MAPPO — Multi-Agent PPO with centralized critic
# Best default for cooperative multi-agent (CTDE)
training:
  algorithm: mappo
  hyperparams:
    lr: 0.0003
    gamma: 0.99
    gae_lambda: 0.95
    clip_epsilon: 0.2
    entropy_coef: 0.01
    n_steps: 2048
    batch_size: 64
    n_epochs: 10
